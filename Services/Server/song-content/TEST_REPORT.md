# 验证记录

## 静态检查

- Node.js 语法：服务端、集成模块和 Agent 管理脚本。
- Swift 语法解析：歌曲内容模型、接口、缓存、ViewModel、详情页以及三类 App Agent 调用链。
- HTML 解析：Agent 管理页面结构和脚本引用 ID。
- 页面实测：桌面 1440 px 与移动端 390 px，无横向溢出；线上 App AI 配置成功回填。
- SQLite：迁移、约束、RBAC、配置版本和内存数据库冒烟。
- Shell：备份和恢复脚本语法。
- Node 自动化测试：34 项通过，覆盖 Agent 配置升级、重试上限、内容补全与增强、自动审核、熔断、持久化和来源清洗。

## 生产检查

- 现有 token-admin PM2 进程保持在线。
- 原管理首页和 Agent 管理入口均通过 HTTPS 返回成功。
- 未授权公共内容接口返回 401。
- 有效 App Token 可获取歌曲内容公开配置和歌曲详情。
- NCM 平台歌曲 ID 可解析为统一歌曲实体。
- Agent 管理读取到与 App 相同的 AI 协议、接口、模型、超时、Key 状态和用量限制。
- 当前代码的公开配置 schema 2 只返回音效调音、听歌洞察和每日问候三类 Agent；旧内置版本会升级到 v28 / v3 / v2，并参与 ETag 和 App 本地缓存失效。
- Agent 管理页面保留三类提示词编辑区，服务端测试覆盖系统提示词、32 段提示词、用户模板、温度、Token、超时和单次任务最大尝试。
- 音乐幕后内容 Agent v7 已覆盖空栏目补全、过短栏目增强、Token 预算门禁和“仅更完整结果可替换”规则。
- QCM《暗号》真实生产任务已完成：保存歌曲、歌手、专辑、平台 MID、封面、来源和四类正文，内容自动校验并发布。
- 对同一 QCM 歌曲再次调用 ensure 返回原发布版本，`generation` 为空，生成任务总数未增加。
- 首次生成失败会复用失败任务，不会因重复打开页面持续创建 AI 请求；后台人工重试会把尝试次数归零。
- Agent 管理静态检测无反模式；线上 CSS 已确认七个标签独立布局，权限表单采用 `min-width: 0` 与响应式单列防溢出。
- 原混淆 AI 配置运行文件哈希未变化。
- Nginx 配置检查通过。

## 未执行

- 按需求方要求未运行 App/Xcode 构建。
- 本轮 Agent 升级未部署到生产服务器。
